{
  "name": "grapheon",
  "version": "0.1.0",
  "private": true,
  "description": "Graph-native code intelligence. Graphify extracts, the Atlas renders.",
  "workspaces": [
    "extract",
    "pipeline",
    "web"
  ],
  "scripts": {
    "build:graph": "node pipeline/build.js",
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "test": "npm run test --workspace web && node --test extract/extract.test.js",
    "sync:android": "npm run build && cap sync android"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "dependencies": {
    "@capacitor/app": "^8.1.1"
  }
}

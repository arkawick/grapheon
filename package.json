{
  "name": "grapheon",
  "version": "0.1.0",
  "private": true,
  "description": "Graph-native code intelligence. Graphify extracts, the Atlas renders.",
  "workspaces": [
    "extract",
    "pipeline",
    "web"
  ],
  "scripts": {
    "build:graph": "node pipeline/build.js",
    "dev": "npm run dev --workspace web",
    "build": "npm run build --workspace web",
    "test": "npm run test --workspace web"
  }
}
